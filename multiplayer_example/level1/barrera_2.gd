extends TileMapLayer

func _ready() -> void:
	# Oculta la capa visualmente, pero mantiene las colisiones activas
	visible = false

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
